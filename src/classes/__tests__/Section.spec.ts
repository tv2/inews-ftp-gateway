// import { SheetSection, SheetSectionDiffWithType } from '../Section'
// import { SheetStory } from '../Story'

describe('Items', () => {
	beforeEach(() => {
		jest.clearAllMocks()
	})

	it('Fake', () => {
		expect(true).toBeTruthy()
	})

	// it('Add story works', () => {
	// 	let a = new SheetSection('orderId123', 'sectionId123', 1, 'Order Name', false)

	// 	expect(a.stories.length).toBe(0)

	// 	let storyOne = new SheetStory('type', 'sectionId', 'storyId1', 0, 'name', false, 'script')
	// 	let storyTwo = new SheetStory('type', 'sectionId', 'storyId2', 0, 'name', false, 'script')
	// 	a.addStories([storyOne])
	// 	expect(a.stories.length).toBe(1)
	// 	expect(a.stories.indexOf(storyOne)).toBe(0)
	// 	a.addStory(storyTwo)
	// 	expect(a.stories.length).toBe(2)
	// 	expect(a.stories.indexOf(storyOne)).toBe(0)
	// 	expect(a.stories.indexOf(storyTwo)).toBe(1)

	// })
})
